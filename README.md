# AI Excel (Stage 1)

## Dev setup (WSL-friendly)

```bash
# from ai-excel/
npm install                # installs root dev deps (concurrently)
cd server && npm install   # backend deps
cd ../client && npm install  # frontend deps
